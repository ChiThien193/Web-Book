/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['example.com'], // Thêm các hostname mà bạn muốn sử dụng cho hình ảnh
    },
  };
  
  export default nextConfig;
  